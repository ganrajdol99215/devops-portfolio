 🌐 Portfolio Website

The main website (`index.html` + `style.css`) showcases:
- My DevOps skills and experience.
- A workflow diagram (see `placeholder.png`).
- Custom domain configuration (`CNAME`).

You can visit the live site at:
>[This is an external link to genome.gov](https://www.genome.gov/)
## 🛠️ Node.js CI/CD Project

Inside the `devops-cicd-node` folder is a sample Node.js application:
- `index.js`: Simple Node.js server.
- `package.json`: Project metadata and dependencies.

This app demonstrates:
- Continuous Integration (CI) using GitHub Actions.
- Continuous Deployment (CD) to cloud platforms Render.
